const links = {
  meals: [
    {
      id: 1,
      nome: 'bebida',
    },
  ],
};

const linksDrinks = {
  drinks: [
    {
      id: 1,
      nome: 'bebida',
    },
  ],
};

console.log(Object.keys(links)[0])

console.log(Object.keys(linksDrinks)[0])

console.log(links[Object.keys(links)[0]])